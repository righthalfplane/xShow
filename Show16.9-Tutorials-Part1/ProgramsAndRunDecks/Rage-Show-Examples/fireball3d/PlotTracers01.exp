{/* export *//* does a time history plot of tracer data */	cellplotset("s");	plotgetlimits(s,xmin,ymin,zmin,xmax,ymax,zmax,nx,ny,nz,time,framecount);		print("framecount = %g\n",framecount);		setframe(framecount-1);		plotgetlimits(s,xmin,ymin,zmin,xmax,ymax,zmax,nx,ny,nz,time,framecount);			num_data=pick(s:tracer_num_data,0);	num_pts=pick(s:tracer_num_pnts,0);	num_vars=pick(s:tracer_num_vars,0);		print("num_data = %g\n",num_data);	print("num_pts = %g\n",num_pts);	print("num_vars = %g\n",num_vars);	num_items=num_vars+1+3; /* time, x, y, z, + num_vars for 3d Calculation */		data=s:tracer_data;		block=num_pts*num_items;		print("block = %g\n",block);		ntime=num_data/block;		print("ntime = %g\n",ntime);		item=0;               /* item   to plot 0 - num_vars-1   */	ntrace=13;            /* tracer to plot 0 - num_pts-1    */		npick=(num_items-num_vars)+item+ntrace*num_items;		y=size(ntime,1);    setoption(y,"line",1);		x=size(ntime,1);		nl=0;	for(k=0;k<ntime;++k){                               	                         	  	 	  	 	copy(data,nl+npick,y,k,1);  	 		 	copy(data,nl,x,k,1); 	 	 	 	 	 	nl += block;   	   	    }		lineplotout(y,x);	}