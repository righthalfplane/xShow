{/*    Open this file into a Show expression window      */	plotgetlimits(d,xmin,ymin,zmin,xmax,ymax,zmax,xsize,ysize,zsize,time);	print("xmin %g ",xmin);	print("ymin %g ",ymin);	print("zmin %g ",zmin);	print("xmax %g ",xmax);	print("ymax %g ",ymax);	print("zmax %g ",zmax);	print("xsize %g ",xsize);	print("ysize %g ",ysize);	print("zsize %g ",zsize);	print("time %g \n",time);		xc=10;	yc=10;	    window("diff_bd",size(xsize,ysize));        x=sizeof(d);    y=sizeof(d);    dx=(xmax-xmin)/xsize;    dy=(ymax-ymin)/ysize;        x=xfill(x,xmin+0.5*dx,dx);    y=yfill(y,ymin+0.5*dy,dy);        r=sqrt((x-xc)*(x-xc)+(y-yc)*(y-yc));        if(r < 10){    	b=10*sin(r)/r;    }else{    	b=0;    }        outs=b;    outd=d;           maxf=max(fabs(outd));   diff_bd=100*fabs(outs-outd)/maxf;       if(r > 9.99){    	diff_bd=0;    }      print("maximum percentage difference %g\n",max(diff_bd));     plotsetlimits(diff_bd,"diff",d);}